{
  "name": "gud",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:migrate": "pnpm --filter @gud/api run db:migrate",
    "db:rollback": "pnpm --filter @gud/api run db:rollback",
    "db:seed": "pnpm --filter @gud/api run db:seed"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0"
}
